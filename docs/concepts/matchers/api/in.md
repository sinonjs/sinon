---
title: sinon.match.in
description: Requires the value to be in the `array`.
---

# `sinon.match.in(array)`

Requires the value to be in the `array`.

<<< ../../../.vitepress/tests/docs/matchers/api/in.test.js
